/**
 * 基础设施自动配置（MySQL/Druid、MyBatis-Plus、Redis 默认集成；S3 / Milvus 按开关启用）。
 *
 * <p><b>说明</b>：由 {@code AutoConfiguration.imports} 加载，不纳入组件扫描，避免缺 jar 时启动失败。
 */
package io.github.jiangbyte.hei.infrastructure.config;
